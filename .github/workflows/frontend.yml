name: Deploy the frontend
on:
  push:
    branches: [main]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the code
        uses: actions/checkout@v2

      - name: Docker login
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and Push
        uses: docker/build-push-action@v4
        with: 
          context: .
          file: ./Dockerfile.frontend
          # build-args: 
          #   - DATABASE_URL=${{ secrets.DATABASE_URL }}
          push: true
          tags: nabin968/heritage-frontend-app:{{ github.sha }}
      # - name: Deploy to ubuntu server
      #   run: |
      #     echo "${{ secrets.SSH_PRIVATE_KEY }}" &> ~/ssh_key
      #     chmod 700 /home/runner/ssh_key
      #     ssh -o StrictHostKeyChecking=no -i ~/ssh_key root@37.27.182.103 -t "docker stop user_backend && docker run --name user_backend -d -p 8080:8080 nabin968/heritage-frontend-app:${{ github.sha }}
      
